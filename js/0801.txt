0801
자바스크립트 객체

 

객체의 개념

속성과 메소드로 구성

객체를 사용하기 위해서는 객체를 생성해야 함

하나의 클래스로 여러 개의 객체 생성 가능

객체 생성 시 new라는 키워드를 사용함

var 객체 = new 클래스 이름()

 

-Collection-

Date 객체

날짜와 시간을 사용할 수 있는 내장 객체

객체 생성은 new라는 연산자를 통해 해야 함    

 

 

String 객체

문자열을 객체화해서 사용

new 연산자를 사용하여 인스턴스를 생성하기도 하지만 new 연산자 없이 바로 " "안에 문자열을 대입해도 똑같은 결과를 가져옴

문자열끼리 합치기: 더하기 연산으로 

substring(시작 인덱스번호, 끝 인덱스번호)

substr(시작 인덱스, 추출 갯수)

 

==

타입 고려하지 않고 값만 비교

===

타입 고려

 

 

Array 객체

new 연산자를 사용하여 인스턴스 생성

var 객체 = new Array(안에 초깃값 넣어도);

 

 

Function 객체

함수를 객체(변수)처럼 사용할 수 있다는 의미

new 연산자를 사용하여 Function 객체 인스턴스 생성

Function 객체에 여러 개의 전달 인자를 대입하기도 하고 계산식을 대입하기도 함

 

new 쓰면 무조건 힙 메모리

함수가 변수에 대입

 

 

Number 객체

숫자형 타입 객체

Number 객체의 제공 속성을 사용할 경우나 문자열을 숫자로 형변환하는 경우 사용

 

 

Math 객체

 

 

 

-Collection-

 

Window 객체

객체 계층 구조의 최상위에 존재

윈도우나 프레임을 의미

Window 객체를 기점으로 document, navigator, object 객체와 같은 하위 객체들이 갈라져 나옴

 

setinterval

주기적으로 수행할 시간마다 함수나 명령을 수행하도록